<!--pages/wxs/wxs.wxml-->
<!-- <view>3.1415926535</view> -->

<!-- wxs的定义方式 -->
<!-- 1.直接在wxml中定义 -->
<!-- <wxs module="info">
  var message = "Hello World";
  var name="eatfinger";

  function sum(num1,num2){
    return num1+num2
  }

  module.exports={
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->
<!-- 2. 定义在单独wxs文件中，再使用<wxs>标签进行导入 -->
<!-- 不能使用绝对路径，必须使用相对路径 -->
<wxs src="../../wxs/info.wxs" module="info"/>
<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20,30)}}</view>

<!-- 3. wxs的使用 -->
<wxs src="../../wxs/format.wxs" module="format"/>

<view>{{format.priceFormat(price,3)}}</view>
<view>{{format.dateFormat(timer,'yyy-MM-dd hh:mm:ss')}}</view>
