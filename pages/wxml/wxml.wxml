<!--pages/wxml/wxml.wxml-->
<!-- 1. wxml的格式 -->
<!-- 单双标签/区分大小写 -->

<!-- 2. Mustache -->
<view>{{message}}</view>
<view>{{age>=18?'成年人':'未成年'}}</view>

<view>{{nowTime}}</view>

<button size="mini" bindtap="handleChangColor">切换颜色</button>
<view class='box {{isActive?"active":""}}'>哈哈哈</view>

<view>---------条件判断-----------</view>
<!-- wx:is的使用 -->
<button size="mini" bindtap="handleSwitchShow">切换显示</button>
<view wx:if="{{isShow}}">蛤蛤蛤</view>

<!-- wx:elif/wx:else -->
<button size="mini" bindtap="handleIncrenment">分数递增6</button>
<view wx:if="{{score>=90}}">优秀</view>
<view wx:elif="{{score>=80}}">良好</view>
<view wx:elif="{{score>=60}}">及格</view>
<view wx:else>不及格</view>

<!-- hidden属性 -->
<view hidden="{{false}}">我是hidden属性控制的内容</view>

<!-- wx:if和hidden在隐藏组件时有什么区别 -->
<!-- 
  区别：
  hidden: 将一个组件隐藏起来时，该组件依然是存在（display：none）
  wx:if: 将一个组件隐藏起来时，该组件根本不存在（压根没有创建）
  选择：
  如果显示和隐藏切换的频率非常高，选hidden
  如果显示和隐藏切换的频率非常低，选wx:if
 -->
<view>----------列表渲染--------------</view>
<view hidden="{{true}}">我是通过hidden控制的内容</view>
<view wx:if="{{false}}">我是通过wx:if控制的内容</view>

<!-- <view>---------------------</view> -->
<!-- 1. wx:for的回顾 -->
<!-- 1.1 遍历数组/字符串/数字  item表示数组里的一项-->
<!-- <view wx:for="{{['abc','cba','nba']}}">{{item}} {{index}}</view>
<view wx:for="eatfinger">{{item}} {{index}}</view>
<view wx:for="{{9}}">{{item}}</view> -->

<!-- block标签（不是组件） -->
<!-- <block wx:if="{{isShow}}">smile</block>
<block wx:for="{{9}}">smile</block> -->

<!-- 3. item、index起名字 -->
<!-- <view wx:for='{{movies}}' wx:for-item="movie" wx:for-index="i">{{movie}} {{i}}</view>

<block wx:for="{{nums}}" wx:for-item="innerNums">
  <block wx:for="{{innerNums}}">
    <view>{{item}}</view>
  </block>
</block> -->

<!-- 4. key的作用 -->
<!-- <view class="container">
  <view wx:for="{{letters}}">{{item}}</view>
</view> -->

<view>---------template---------</view>
<!-- 
  之前的时候小程序不支持自定义组件，为了进行代码的复用：template
 -->
 <!-- 模板中的内容，在没有被使用前，是不会进行任何渲染的 -->
<!-- <template name="contentItem">
  <button size="mini">{{btnText}}</button>
  <view>{{content}}</view>
</template> -->
<import src="../wxml/template.wxml"/>

<template is="contentItem" data="{{btnText: '按钮',content: '木落'}}"/>
<template is="contentItem" data="{{btnText: '警告',content: '北风'}}"/>
<template is="contentItem" data="{{btnText: '点击',content: '江雪'}}"/>

